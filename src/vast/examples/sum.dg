fun sum(numbers: i32[]): i32 {
    var result = 0
    for i in numbers {
        result += i
    }
    result
}

val numbers = [1, 2, 3, 4, 5, 6]

export fun test1() = sum(numbers)
export fun test2() = sum(numbers[3:])
export fun test3() {
    var result = 0
    for i in number.indices {
        result += sum(numbers[i:])
    }
    result
}